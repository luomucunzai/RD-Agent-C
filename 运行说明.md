# RD-Agent 加密货币因子优化运行说明

## 快速启动

### 1. SSH 登录服务器

```bash
ssh li@192.168.1.171
```

### 2. 激活环境并配置变量

```bash
source ~/rdagent_venv/bin/activate
export $(grep -v '^#' .env | xargs)
export PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
```

### 3. 运行因子优化

**因子挖掘（最常用）：**
```bash
python -c "
from rdagent.app.qlib_rd_loop.factor import main
main(path=None, step_n=1, loop_n=1, checkout=False)
"
```

**因子+模型联合优化：**
```bash
python -c "
from rdagent.app.qlib_rd_loop.quant import main
main(path=None, step_n=1, loop_n=1, checkout=False)
"
```

**模型进化（需 torch）：**
```bash
export MODEL_CoSTEER_env_type=local
python -c "
from rdagent.app.qlib_rd_loop.model import main
main(path=None, step_n=1, loop_n=1, checkout=False)
"
```

### 5. 查看结果

日志和结果保存在：
```
git_ignore_folder/RD-Agent_workspace/
```

---

## 一键测试脚本

把以下内容保存为 `test_crypto.sh`：

```bash
#!/bin/bash
source ~/rdagent_venv/bin/activate
cd ~/rdagent_dev
export $(grep -v '^#' .env | xargs)
export PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
export CONDA_DEFAULT_ENV=""

echo "=== 测试 fin_factor ==="
python -c "
import rdagent.scenarios.qlib.experiment.factor_experiment as fe
fe.QlibFactorScenario.get_runtime_environment = lambda self: 'crypto'
from rdagent.app.qlib_rd_loop.factor import main
main(path=None, step_n=1, loop_n=1, checkout=False)
"
```

运行：`bash test_crypto.sh`

---

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `step_n` | 每轮循环的 step 数 | 5 |
| `loop_n` | 最大进化循环次数 | 10 |
| `path` | workspace 路径 | 自动生成 |
| `checkout` | 完成后是否保留 workspace | True |

示例：跑 2 轮循环
```python
main(path=None, step_n=5, loop_n=2, checkout=True)
```

---

## 当前支持的加密货币

- BTCUSDT, ETHUSDT, BNBUSDT, SOLUSDT
- DOGEUSDT, TAOUSDT, 1000PEPEUSDT, HYPEUSDT

数据范围：2024-05-14 ~ 2026-04-30（8 个交易对，717 天）

---

## 恢复 A 股模式

退出加密货币模式只需注释 `.env` 中的 `CRYPTO_MODE=true`：
```bash
# CRYPTO_MODE=true
```

或在运行前：
```bash
unset CRYPTO_MODE
```
