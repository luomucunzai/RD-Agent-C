# RD-Agent 加密货币因子优化运行说明

## 部署到新服务器

### 1. 上传项目文件

```bash
# 将 dev/ 目录上传到服务器任意用户目录
rsync -avz --exclude=venv --exclude=.git --exclude=__pycache__ /path/to/dev/ user@server:~/rdagent_dev/
```

### 2. 安装环境（第一次）

```bash
# Miniconda（如果没有）
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3
~/miniconda3/bin/conda tos accept --channel https://repo.anaconda.com/pkgs/main
~/miniconda3/bin/conda create -n rdagent python=3.10 -y

# 安装依赖
~/miniconda3/envs/rdagent/bin/pip install pyqlib
~/miniconda3/envs/rdagent/bin/pip install -r ~/rdagent_dev/requirements.txt
~/miniconda3/envs/rdagent/bin/pip install -e ~/rdagent_dev --no-deps
```

### 3. 配置 .env

```bash
cd ~/rdagent_dev
cat > .env << 'EOF'
CRYPTO_MODE=true
CHAT_MODEL=deepseek/deepseek-chat
DEEPSEEK_API_KEY=sk-your-deepseek-key
EMBEDDING_MODEL=litellm_proxy/BAAI/bge-m3
LITELLM_PROXY_API_KEY=sf-your-siliconflow-key
LITELLM_PROXY_API_BASE=https://api.siliconflow.cn/v1
EOF
```

### 4. 每次运行前配置环境

```bash
conda activate rdagent
cd ~/rdagent_dev
export $(grep -v '^#' .env | xargs)
export PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
```

配置完环境后，根据需要执行具体命令（见下节）。

---

## 命令说明

### 因子挖掘（最常用）

```bash
rdagent fin_factor
```

自动完成：因子假设生成 → 代码实现 → 代码执行 → 因子评估 → Qlib 回测 → 反馈总结

### 其他场景

```bash
rdagent fin_quant        # 因子+模型联合优化（自动切换）
export MODEL_CoSTEER_env_type=local && rdagent fin_model   # 模型进化（需 torch）
rdagent server_ui --port 19899 --log-dir log  # Web UI 查看日志
```

Web UI 访问地址：`http://192.168.1.171:19899`

---

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `step_n` | 每轮循环的 step 数。流程分为 direct_exp_gen → coding → running → feedback → record 五个步骤，每个步骤内部可能有多次迭代 | 5 |
| `loop_n` | 最大 R&D 进化循环次数。一次完整 loop = 以上 5 步全部完成，loop_n 控制最多做几轮完整的因子挖掘 | 1 |
| `--checkout / --no-checkout` | 完成后是否保留 workspace 日志 | checkout |

**step_n 和 loop_n 的关系：**
```
loop_n=1, step_n=5 → 跑 1 轮完整的 5 步流程
loop_n=2, step_n=5 → 跑 2 轮完整流程（第二轮基于第一轮结果改进）
loop_n=1, step_n=2 → 只跑前 2 步（假设生成 + 编码，不跑回测）
```

**使用示例：**
```bash
# 快速测试（1轮，少量步骤）
rdagent fin_factor --step-n 5 --loop-n 1

# 完整挖掘（多轮迭代）
rdagent fin_factor --step-n 5 --loop-n 3

# 仅生成假设和编码（不跑回测）
rdagent fin_factor --step-n 2
```

---

## 回测策略说明

Qlib 使用 **`TopkDropoutStrategy`** 策略进行交易回测，不是全量交易：

1. **每天**用 LGBModel 预测所有标的的未来收益
2. 按预测值排序，取前 **topk** 名（加密货币默认 **10**，可配置）
3. 从选中的标的中随机丢弃 **n_drop** 个（增加探索性）
4. **只交易选中标的**，未选中的不交易

**参数控制：**

```bash
# 每日只选前 3 个最有信心的币交易
export CRYPTO_TOPK=3
export CRYPTO_N_DROP=0
rdagent fin_factor

# 全部交易（8个币全选）
export CRYPTO_TOPK=10
export CRYPTO_N_DROP=0
```

---

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `step_n` | 每轮循环的 step 数 | 5 |
| `loop_n` | 最大进化循环次数 | 10 |
| `--checkout` | 完成后是否保留 workspace | True |
| `CRYPTO_TOPK` | 每日选币数量 | 10 |
| `CRYPTO_N_DROP` | 每日丢弃数量 | 5 |
| `CRYPTO_ANN_SCALER` | 年化天数 | 365 |

示例：
```bash
rdagent fin_factor --step-n 5 --loop-n 1 --checkout
```

---

## 当前支持的加密货币

- BTCUSDT, ETHUSDT, BNBUSDT, SOLUSDT
- DOGEUSDT, TAOUSDT, 1000PEPEUSDT, HYPEUSDT

数据范围：2024-05-14 ~ 2026-04-30（8 个交易对，717 天，连续无断档）

---

## 一键初始化加密货币数据

```bash
# 全自动：下载CSV → 转H5 → 转Qlib格式 → 配置时间范围
python init_crypto_data.py

# 已有CSV数据时跳过下载
python init_crypto_data.py --skip-download

# 快速测试（少量数据）
python init_crypto_data.py --debug
```

自动完成：
1. 从币安下载 8 个交易对的 2 年日线数据
2. 转换为 H5 格式（MultiIndex, $factor=1.0）
3. 转换为 Qlib 原生 bin 格式（float32）
4. 自动计算 train/valid/test 时间范围
5. 更新 `conf.py` 时间配置

## 因子集切换

系统通过 `CRYPTO_MODE` 自动选择因子集：

| 模式 | 因子集 | 文件 | 适用市场 |
|------|--------|------|---------|
| `CRYPTO_MODE=true` | **CRYPTO_20 / CRYPTO_ALPHA** | `rdagent/utils/crypto_alpha.py` | 加密货币 |
| `CRYPTO_MODE=false` | **ALPHA20 / ALPHA158** | `rdagent/utils/qlib.py` | A 股 |

### CryptoAlpha 因子类别（90+ 因子）

| 类别 | 代表因子 | 说明 |
|------|---------|------|
| K线形态 | CKLEN, CPOS, CKUP | OHLC 位置（无 $vwap 依赖） |
| 动量 | CROC3~21, CLRET3~21 | 简单 + 对数收益率，3~21 天 |
| 波动率 | CSTD, CLSTD 系列 | 价格 + 对数收益率标准差 |
| 趋势强度 | CRSQR5~21 | R² 判定趋势可信度 |
| 量价相关 | CCORR, CCORD 系列 | Log-Log 相关，适合大幅波动 |
| 成交量 | CVMA, CVSTD, CVOLCH | Log 变换，消除量级差异 |
| 涨跌比 | CCNTP, CCNTD | 上涨天数比例 |

### 对比测试

想对比 CryptoAlpha 和 Alpha158 在加密货币上的表现：

```bash
# 1. 跑 CryptoAlpha
export CRYPTO_MODE=true
rdagent fin_factor --step-n 5 --loop-n 1

# 2. 跑 Alpha158（对比）
export CRYPTO_MODE=false
rdagent fin_factor --step-n 5 --loop-n 1
```

结果保存在 `git_ignore_folder/RD-Agent_workspace/` 下，通过 `rdagent server_ui` 查看。

### 恢复 A 股模式

```bash
# 注释 .env 中的 CRYPTO_MODE=true
# 或在运行前
unset CRYPTO_MODE
```
