# 修改计划：RD-Agent 开发版支持加密货币

## 背景

原始 RD-Agent 的 `qlib` 场景（`rdagent/scenarios/qlib/`）是为中国 A 股市场设计的。股票市场与加密货币市场的核心差异：

| 特性 | A股 (原始) | 加密货币 (目标) |
|------|-----------|----------------|
| 交易时间 | 周一至周五 9:30-15:00 | 7x24 小时连续交易 |
| 复权因子 (factor) | 有 `$factor` 列用于前复权/后复权 | 无复权，`$factor=1.0` 不做调整 |
| 日历 | 需日历过滤非交易日 | 不需要日历过滤，数据连续 |
| 标的识别 | `SH600000`、`SZ300059` 等格式 | BTCUSDT、ETHUSDT 等交易对 |

**注意：** `修改说明.md` 中写的路径是 `rdagent/scenarios/quant/`，但实际代码路径为 `rdagent/scenarios/qlib/`。以下所有文件路径均已核对实际源码。

---

## 修改清单（按优先级）

---

### P0：必须修改（不改则功能异常）

#### 修改 1：数据格式描述 —— 说明 7x24

**原因：** LLM 根据数据描述生成因子代码。如果描述中还有周末休市假设，LLM 会生成错误的因子代码。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/factor_data_template/README.md` |
| **计划修改** | 移除 `$factor` 列描述，添加 7x24 说明 |
| **实际执行** | **✅ 完成（方式变更）** 保留了 `$factor` 列并说明"always 1.0 for crypto"；列名从 "stock on that day" 改为通用表述；添加 7x24 说明 |
| **变更原因** | Qlib 回测引擎需要 `$factor` 列存在，设置为 1.0（无复权） |

#### 修改 2：数据目录介绍函数 —— 传递 CRYPTO_MODE

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/utils.py` |
| **计划修改** | `get_data_folder_intro()` 读取 CRYPTO_MODE，追加 7x24 描述 |
| **实际执行** | **✅ 完成** 新增 `generate_data_folder_from_crypto()` 函数（不依赖 Docker，直接本地执行）；修改 `get_data_folder_intro()` 追加说明 |
| **额外工作** | 增加了 `import sys`，用于执行子进程调用 |

#### 修改 3：因子代码输出格式 Prompt —— 移除股票代码引用

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/prompts.yaml` |
| **计划修改** | `qlib_factor_output_format` 段移除股票代码示例 |
| **实际执行** | **✅ 完成** 移除了 `SH600000`/`SZ300059`/`SZ000001` 等代码，改为 INSTR_A/B/C 通用格式；`qlib_factor_background` 将 "on a day" 改为 "on a day (or period)"；`qlib_factor/qlib_model_experiment_setting` 支持 `crypto_mode` 条件显示 |

---

### P0：日历系统（数据连续性）

#### 修改 4：因子数据处理 —— 跳过 1 分钟数据检测

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/utils.py:121-124` |
| **计划修改** | CRYPTO_MODE 时跳过 `pd.Timedelta(minutes=1)` 检测 |
| **实际执行** | **✅ 完成** 与计划一致 |

#### 修改 5：Qlib 配置模板 —— 替换市场、标的参数

| 项 | 内容 |
|---|---|
| **文件** | 计划：`factor_template/conf_baseline.yaml` + `conf_combined_factors.yaml` + `conf_combined_factors_sota_model.yaml` |
| | **实际：额外增加了 2 个** `model_template/conf_baseline_factors_model.yaml` + `conf_sota_factors_model.yaml` |
| **计划修改** | 将 YAML 硬编码参数改为模板变量 |
| **实际执行** | **✅ 完成（范围扩大）** 共修改 5 个 YAML 文件（比计划多 2 个），所有 A 股特定参数（`region`, `market`, `benchmark`, `ann_scaler`, `limit_threshold`, `min_cost` 等）均改为 Jinja2 模板变量 |
| **变更原因** | 模型模板中也包含相同的硬编码参数，一并参数化避免遗漏 |

---

### P1：建议修改

#### 修改 6：时间范围配置 —— CRYPTO_MODE 配置

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/app/qlib_rd_loop/conf.py` |
| **计划修改** | 增加 CRYPTO_MODE 判断，切换时间范围 |
| **实际执行** | **✅ 完成（数值调整）** 最初设 2023-01-01 开始，但币安数据最早到 2024-05-14，最终调整为：train=2024-05-14~2025-02-28, valid=2025-03-01~2025-08-31, test=2025-09-01~2026-04-30 |

---

### P2：可选修改

#### 修改 7：Runner 环境 —— 回测路径处理

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/factor_runner.py` |
| **计划修改** | `develop()` 中根据 CRYPTO_MODE 传入不同参数 |
| **实际执行** | **✅ 完成** 移除了不必要的 `provider_uri` 覆盖（沿用默认 `~/.qlib/qlib_data/cn_data`），仅覆盖 market/benchmark/ann_scaler 等参数 |

---

### P3：进一步优化

#### 修改 8：Hypothesis / Feedback Prompt —— 增加加密货币指导

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/prompts.yaml` |
| **计划修改** | 在 prompt 末尾增加加密货币指导 |
| **实际执行** | **✅ 完成** `factor_hypothesis_specification` 添加第 6 条 CRYPTO_MODE；`factor_feedback_generation.system` 末尾添加 CRYPTO_MODE 指导 |

#### 修改 9：Feedback 分析 —— 指标适配

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/feedback.py` |
| **计划修改** | 根据 CRYPTO_MODE 选择不同的指标集 |
| **实际执行** | **✅ 完成** `IMPORTANT_METRICS` 切换为 `['IC', 'annualized_return', 'max_drawdown']` |

#### 修改 10：.env 配置

| 项 | 内容 |
|---|---|
| **文件** | `dev/.env` |
| **计划修改** | 添加 `CRYPTO_MODE=true` |
| **实际执行** | **✅ 完成** 添加 CRYPTO_MODE + DeepSeek Chat + SiliconFlow Embedding 配置 |

---

### 额外增加的修改（计划中未列出）

| # | 文件 | 修改内容 | 原因 |
|---|------|----------|------|
| 11 | `rdagent/scenarios/qlib/experiment/factor_data_template/generate_crypto.py` | **新增** 币安 CSV 转 H5 脚本 | 原计划依赖现有脚本，实际需新建加密货币专用的数据转换工具 |
| 12 | `rdagent/oai/backend/litellm.py` | **修复** Embedding 兼容性 | LiteLLM 向 SiliconFlow 发请求时带了 `encoding_format: None`，SiliconFlow 报错 400。改为检测 `litellm_proxy/` 前缀时用 OpenAI 客户端直连 |
| 13 | `rdagent/scenarios/qlib/experiment/factor_experiment.py` | 给模板传入 `crypto_mode`/`market_name` | 使 experiment_setting 模板能显示 CRYPTO_MODE 状态 |
| 14 | `rdagent/scenarios/qlib/experiment/model_experiment.py` | 同上 | 同上 |
| 15 | `rdagent/scenarios/qlib/experiment/quant_experiment.py` | 同上 | 同上 |

---

### 未完成项

| # | 内容 | 原因 | 后续计划 |
|---|------|------|----------|
| 1 | 下载数据后自动部署到 `cn_data` | 目前手动 cp，`generate_data_folder_from_crypto()` 可自动复制到 factor 源数据目录，但未处理 `~/.qlib/qlib_data/cn_data/` | 可在 `generate_data_folder_from_crypto()` 中增加向 cn_data 的自动复制 |
| 2 | `factor_experiment_loader/prompts.yaml` 中的 "China A-share" 描述 | 该模块用于从研究报告提取因子，加密货币场景暂不使用 | 暂不处理，如需使用再修改 |
| 3 | 非 Qlib 回测方案 | 目前仍使用 Qlib 回测（conda 模式） | 如需替换回测引擎需重写 `QlibFactorRunner` + `QlibFBWorkspace` |

---

## 通用方案：CRYPTO_MODE 环境变量

所有修改都围绕统一的环境变量 `CRYPTO_MODE`（可通过 `.env` 设置）：

- `CRYPTO_MODE=true` → 启用加密货币模式（$factor=1.0、7x24 连续时间、ann_scaler=365）
- `CRYPTO_MODE=false` 或未设置 → 保持原有的 A 股模式

这样不影响原有的量化金融场景，通过环境变量切换。

---

## 数据来源

使用 `downdata.py` 从币安（Binance）下载永续合约日线数据（`binance_futures_data/`），`generate_crypto.py` 转成 H5 格式。

**数据路径：** H5 文件同时部署到：
- `~/.qlib/qlib_data/cn_data/daily_pv.h5`（Qlib 回测引擎）
- `git_ignore_folder/factor_implementation_source_data/daily_pv.h5`（因子代码源数据）

**数据格式：** MultiIndex (datetime, instrument)，6 列（$open/$close/$high/$low/$volume/$factor），$factor=1.0
