# 修改计划：RD-Agent 开发版支持加密货币

## 背景

原始 RD-Agent 的 `qlib` 场景（`rdagent/scenarios/qlib/`）是为中国 A 股市场设计的。股票市场与加密货币市场的核心差异：

| 特性 | A股 (原始) | 加密货币 (目标) |
|------|-----------|----------------|
| 交易时间 | 周一至周五 9:30-15:00 | 7x24 小时连续交易 |
| 复权因子 (factor) | 有 `$factor` 列用于前复权/后复权 | 无复权概念 |
| 日历 | 需日历过滤非交易日 | 不需要日历过滤，数据连续 |
| 标的识别 | `SH600000`、`SZ300059` 等格式 | 无此格式要求 |

**注意：** `修改说明.md` 中写的路径是 `rdagent/scenarios/quant/`，但实际代码路径为 `rdagent/scenarios/qlib/`。以下所有文件路径均已核对实际源码。

---

## 修改清单（按优先级）

---

### P0：必须修改（不改则功能异常）

#### 修改 1：数据格式描述 —— 移除 factor 列，说明 7x24

**原因：** LLM 根据数据描述生成因子代码。如果描述中还有 `$factor` 列和周末休市假设，LLM 会生成错误的因子代码（引用不存在的列、跳过周末数据）。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/factor_data_template/README.md` |
| **修改** | 第 24 行：移除 `$factor: factor value of the stock on that day.`，添加说明"数据为连续时间戳，无周末休市" |
| **工作量** | 极小（2 行文字） |

#### 修改 2：数据目录介绍函数 —— 传递 CRYPTO_MODE

**原因：** `get_data_folder_intro()` 读取 h5 文件后生成描述文本给 LLM。该描述包含 `factor.bin` 等文件的索引信息。需要让其根据 CRYPTO_MODE 判断是否跳过周末并告知 LLM。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/utils.py` |
| **修改** | `get_data_folder_intro()` 函数：读取 CRYPTO_MODE 环境变量，若开启则在返回的描述文本中标记"数据无 $factor 列、7x24 连续交易" |
| **工作量** | 小（约 10 行） |

#### 修改 3：因子代码输出格式 Prompt —— 移除 factor 引用

**原因：** `prompts.yaml` 定义了因子代码的输出格式示例，其中 `qlib_factor_output_format` 展示了包含 `SH600000`、`SZ300059` 等标的索引格式，以及可能涉及 factor 列的假设。加密货币不需要这些格式。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/prompts.yaml` |
| **修改** | `qlib_factor_output_format` 段：将示例中的股票代码格式改为通用格式；`qlib_factor_interface` 段确保不引用 factor 列 |
| **工作量** | 小（修改模板字符串） |

---

### P0：日历系统（数据连续性）

#### 修改 4：因子数据处理 —— 跳过 1 分钟数据检测

**原因：** `_process_message_and_df()` 函数（`rdagent/scenarios/qlib/developer/utils.py` 第 121-124 行）有一处检测：

```python
time_diff = df.index.get_level_values("datetime").to_series().diff().dropna().unique()
if pd.Timedelta(minutes=1) in time_diff:
    logger.warning(f"Factor data from {source_name} is not generated.")
    return error_message
```

这条代码会把包含 1 分钟数据的 dataframe **直接丢弃**，因为原始逻辑认为 1 分钟数据是无效的（股票按日频计算因子）。加密货币的因子代码可能在不同时间粒度上运行，这条检测对加密货币是错误的。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/utils.py:121-124` |
| **修改** | 在检测 `pd.Timedelta(minutes=1)` 时增加 CRYPTO_MODE 判断，若开启则跳过此检测 |
| **工作量** | 小（约 3 行） |

#### 修改 5：Qlib 配置模板 —— 替换市场、标的参数

**原因：** Qlib 配置文件中的 `provider_uri: "~/.qlib/qlib_data/cn_data"`、`market: csi300`、`benchmark: SH000300` 等中国 A 股参数。如果加密货币不使用 Qlib 回测，需要在这些配置中添加 CRYPTO_MODE 分支。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/experiment/factor_template/conf_baseline.yaml` |
|  | `rdagent/scenarios/qlib/experiment/factor_template/conf_combined_factors.yaml` |
|  | `rdagent/scenarios/qlib/experiment/factor_template/conf_combined_factors_sota_model.yaml` |
| **修改** | 添加条件判断或创建 crypto 版本的配置文件，使用不同的 provider_uri、region、market 设置 |
| **工作量** | 中（3 个 yaml 文件） |

---

### P1：建议修改

#### 修改 6：时间范围配置 —— CRYPTO_MODE 配置

**原因：** `conf.py` 中的 `FactorBasePropSetting` 和 `QuantBasePropSetting` 定义了 train/valid/test 时间范围，这些范围是 A 股特定的（2008-2020 年）。加密货币需要不同的时间配置。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/app/qlib_rd_loop/conf.py` |
| **修改** | 在已有设置中增加 `CRYPTO_MODE` 判断，为加密货币设置不同的默认时间范围；或在 `.env` 中用不同的环境变量前缀（如 `QLIB_FACTOR_` vs `CRYPTO_FACTOR_`） |
| **工作量** | 小（约 15 行） |

---

### P2：可选修改

#### 修改 7：Runner 环境 —— 回测路径处理

**原因：** `QlibFactorRunner.develop()` 中硬编码了 Qlib 相关的回测流程。如果加密货币使用不同的回测引擎，需要增加 CRYPTO_MODE 分支。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/factor_runner.py` |
| **修改** | `develop()` 方法中根据 CRYPTO_MODE 决定使用 Qlib 配置还是加密货币配置 |
| **工作量** | 中（约 30 行） |

---

### P3：进一步优化

#### 修改 8：Hypothesis / Feedback Prompt —— 增加加密货币指导

**原因：** Hypothesis 生成的 system prompt 和 feedback 生成 prompt 目前使用纯股票术语。增加加密货币特殊的指导可以提高 LLM 生成质量。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/prompts.yaml` |
| | - `factor_hypothesis_specification` 段 |
| | - `factor_feedback_generation.system` 段 |
| **修改** | 在 prompt 末尾增加 CRYPTO_MODE 相关的指导（如"无复权因子概念"、"市场 7x24 运行"） |
| **工作量** | 小（各加 2-3 句说明） |

#### 修改 9：Feedback 分析 —— 识别 factor 相关错误

**原因：** `QlibFactorExperiment2Feedback.generate_feedback()` 中 `IMPORTANT_METRICS` 包含 Qlib 特定的回测指标。加密货币回测结果可能有不同的指标名称。

| 项 | 内容 |
|---|---|
| **文件** | `rdagent/scenarios/qlib/developer/feedback.py` |
| **修改** | 根据 CRYPTO_MODE 选择不同的指标集，检测 feedback 中是否有"factor"相关错误 |
| **工作量** | 小（约 10 行） |

#### 修改 10：.env 配置

| 项 | 内容 |
|---|---|
| **文件** | `.env`（位于 dev/ 根目录） |
| **添加** | `CRYPTO_MODE=true` |
| **工作量** | 极小 |

---

## 通用方案：CRYPTO_MODE 环境变量

所有修改都围绕一个统一的环境变量 `CRYPTO_MODE`（可通过 `.env` 设置）：

- `CRYPTO_MODE=true` → 启用加密货币模式（无 factor、7x24 连续时间）
- `CRYPTO_MODE=false` 或未设置 → 保持原有的 A 股模式

这样不影响原有的量化金融场景，通过环境变量切换。

---

## 数据来源

`修改说明.md` 中提及使用 `downdata.py` 从币安（Binance）下载加密货币历史数据。该数据文件位于开发版根目录，尚未检查该文件是否存在。
