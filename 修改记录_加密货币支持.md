# RD-Agent 加密货币支持修改记录

## 修改思路

RD-Agent 原有的 `qlib` 场景（`rdagent/scenarios/qlib/`）是为中国 A 股市场设计的，包含大量股票特定假设。加密货币与 A 股的核心差异：

| 特性 | A 股 | 加密货币 |
|------|------|---------|
| 交易时间 | 周一至周五 9:30-15:00 | 7x24 小时连续交易 |
| 复权因子 | `$factor` 用于前复权/后复权 | 无复权概念，`$factor=1.0`（恒等于1，不做调整） |
| 交易日历 | 需日历过滤非交易日 | 每天都是交易日，数据连续 |
| 年化天数 | 252 个交易日 | 365 个自然日 |
| 标的识别 | `SH600000`、`SZ300059` 等股票代码 | `BTCUSDT`、`ETHUSDT` 等交易对 |
| 涨跌停限制 | 10%（中国 A 股） | 无限制 |
| 最低佣金 | 5 元 | 无最低佣金 |

所有修改围绕统一的环境变量 `CRYPTO_MODE=true` 进行开关，不影响原有的 A 股场景。

---

## 修改文件清单

### 1. 数据格式与生成（P0 - 必须）

#### 1.1 `rdagent/scenarios/qlib/experiment/factor_data_template/README.md`
- 列名从 "stock on that day" 改为 "instrument on that period"
- 添加 `$factor` 列描述（"always 1.0 for crypto"）
- 添加 CRYPTO_MODE 说明

#### 1.2 `rdagent/scenarios/qlib/experiment/utils.py`
- **新增** `generate_data_folder_from_crypto()` 函数：直接调用 `generate_crypto.py`（不通过 Docker）
- **修改** `get_data_folder_intro()`：CRYPTO_MODE 时调用 `generate_data_folder_from_crypto()` 而非 `generate_data_folder_from_qlib()`
- **修改** `get_data_folder_intro()`：CRYPTO_MODE 时追加"7x24 连续交易、$factor=1.0"说明

#### 1.3 `rdagent/scenarios/qlib/experiment/factor_data_template/generate_crypto.py`
- **新增文件**：从币安 CSV 生成 H5 数据的脚本
- 输出格式：MultiIndex (datetime, instrument)，列：`$open, $close, $high, $low, $volume, $factor`
- `$factor` 全部为 1.0（加密货币无复权）
- **数据路径**：生成到 `factor_data_template/daily_pv_all.h5`，然后复制到：
  - `~/.qlib/qlib_data/cn_data/daily_pv.h5`（Qlib 回测引擎）
  - `git_ignore_folder/factor_implementation_source_data/daily_pv.h5`（因子代码源数据）

---

### 2. 交易日历限制（P0 - 必须）

#### 2.1 `rdagent/scenarios/qlib/developer/utils.py`
- **修改** `_process_message_and_df()`：CRYPTO_MODE 时跳过 1 分钟数据检测（加密货币可能使用不同时间粒度）

---

### 3. Qlib 配置模板参数化（P0 - 必须）

以下 5 个 YAML 文件将硬编码的中国 A 股参数改为 Jinja2 模板变量：

- `factor_template/conf_baseline.yaml`
- `factor_template/conf_combined_factors.yaml`
- `factor_template/conf_combined_factors_sota_model.yaml`
- `model_template/conf_baseline_factors_model.yaml`
- `model_template/conf_sota_factors_model.yaml`

**参数化内容**：`provider_uri`, `region`, `market`, `benchmark`, `ann_scaler`, `topk`, `n_drop`, `account`, `limit_threshold`, `open_cost`, `close_cost`, `min_cost`

---

### 4. Runner 执行环境

#### 4.1 `rdagent/scenarios/qlib/developer/factor_runner.py`
- CRYPTO_MODE 时传入：`region=global`, `market=all`, `benchmark=BTCUSDT`
- `ann_scaler=365`（自然日），`limit_threshold=0`（无涨跌停），`min_cost=0`（无最低佣金）
- 数据路径沿用默认 `~/.qlib/qlib_data/cn_data`

---

### 5. 时间范围配置

#### 5.1 `rdagent/app/qlib_rd_loop/conf.py`
- CRYPTO_MODE 时默认时间范围（匹配币安 2 年数据）：
  - train: 2024-05-14 ~ 2025-02-28
  - valid: 2025-03-01 ~ 2025-08-31
  - test: 2025-09-01 ~ 2026-04-30

---

### 6. Prompt 模板修改

#### 6.1 `rdagent/scenarios/qlib/experiment/prompts.yaml`
- `qlib_factor_output_format`：移除 `SH600000`/`SZ300059` 等股票代码示例
- `qlib_factor_background`：从 "on a day" 改为 "on a day (or period)"
- `qlib_factor/qlib_model_experiment_setting`：支持 `crypto_mode` 条件显示

#### 6.2 `rdagent/scenarios/qlib/prompts.yaml`
- `factor_hypothesis_specification`：添加 CRYPTO_MODE 指导
- `factor_feedback_generation.system`：末尾添加 CRYPTO_MODE 指导

---

### 7. Feedback 指标适配

#### 7.1 `rdagent/scenarios/qlib/developer/feedback.py`
- CRYPTO_MODE 时 `IMPORTANT_METRICS` 改为 `['IC', 'annualized_return', 'max_drawdown']`

---

### 8. Scenario 模板变量传递

#### 8.1 `rdagent/scenarios/qlib/experiment/factor_experiment.py`
#### 8.2 `rdagent/scenarios/qlib/experiment/model_experiment.py`
#### 8.3 `rdagent/scenarios/qlib/experiment/quant_experiment.py`
- 给 experiment_setting 模板传入 `crypto_mode` 和 `market_name` 变量

---

### 9. Embedding 兼容性修复

#### 9.1 `rdagent/oai/backend/litellm.py`
- **修改** `_create_embedding_inner_function()`：检测 `litellm_proxy/` 前缀时使用 OpenAI 客户端直连（绕过 LiteLLM 的 encoding_format 参数问题）
- 原因：LiteLLM 向 SiliconFlow 发送 embedding 请求时带了 `encoding_format: None`，SiliconFlow 不接受此参数

---

### 10. 配置文件

#### 10.1 `dev/.env`
- 设置 `CRYPTO_MODE=true`
- DeepSeek Chat + SiliconFlow Embedding 配置

---

## 服务器部署状态

| 项目 | 信息 |
|------|------|
| **服务器** | 192.168.1.171（Ubuntu, Python 3.10） |
| **SSH 登录** | 密钥认证（免密码） |
| **虚拟环境** | `~/rdagent_venv`（Python venv） |
| **项目路径** | `~/rdagent_dev/` |
| **Qlib** | `pip install qlib` |
| **LLM Chat** | DeepSeek API ✅ |
| **LLM Embedding** | SiliconFlow bge-large-en-v1.5 ✅ |
| **币安数据** | 8 个交易对（BTC/ETH/BNB/SOL/DOGE/TAO/PEPE/HYPE） |
| **数据范围** | 2024-05-14 ~ 2026-04-30，717 条/对 |
| **H5 数据** | `~/.qlib/qlib_data/cn_data/daily_pv.h5`（282KB） |

### 部署步骤

```bash
# 1. 登录
ssh li@192.168.1.171

# 2. 激活环境
source ~/rdagent_venv/bin/activate
export $(grep -v '^#' ~/rdagent_dev/.env | xargs)

# 3. 下载数据
cd ~/rdagent_binance && python downdata.py

# 4. 转 H5
cd ~/rdagent_dev
python rdagent/scenarios/qlib/experiment/factor_data_template/generate_crypto.py

# 5. 部署到数据目录
cp rdagent/.../factor_data_template/daily_pv_all.h5 ~/.qlib/qlib_data/cn_data/daily_pv.h5
cp rdagent/.../factor_data_template/daily_pv_all.h5 git_ignore_folder/factor_implementation_source_data/daily_pv.h5

# 6. 运行
rdagent fin_factor
```

### `.env` 配置

```bash
CRYPTO_MODE=true
CHAT_MODEL=deepseek/deepseek-chat
DEEPSEEK_API_KEY=sk-xxx
EMBEDDING_MODEL=litellm_proxy/BAAI/bge-large-en-v1.5
LITELLM_PROXY_API_KEY=sf-xxx
LITELLM_PROXY_API_BASE=https://api.siliconflow.cn/v1
```

取消 `CRYPTO_MODE=true` 即可恢复 A 股模式。
